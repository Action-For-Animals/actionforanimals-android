<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="32dp"
    android:layout_height="32dp">

    <!-- Circular image for representative photo -->
    <ImageView
        android:id="@+id/avatar_image"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/circle_avatar_background"
        android:scaleType="centerCrop"
        android:contentDescription="Representative photo" />

    <!-- Fallback initials (only shown if image fails to load) -->
    <TextView
        android:id="@+id/avatar_initials"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="@android:color/white"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:visibility="gone" />

    <!-- Green checkmark overlay for completed calls -->
    <ImageView
        android:id="@+id/checkmark_overlay"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/green_checkmark_circle"
        android:src="@drawable/checked_small"
        android:scaleType="center"
        android:visibility="gone"
        android:contentDescription="Called" />

</FrameLayout>